
steps:

  - checkout: self
    submodules: recursive
    lfs: true
    path: .




  - script: |
        git lfs install
        git lfs pull
        git submodule update --init
    displayName: git-deps

  - script: |
        cd deploy
        # use absolute paths:
        ./conda/install_conda.sh
    displayName: build-conda






        # - script: |
        #       cd ./deploy
        #       ./install_all.sh
        #   displayName: build
